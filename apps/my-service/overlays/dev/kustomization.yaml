apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
  - ../../base
patchesStrategicMerge:
  - patch-replicas.yaml
# Você pode adicionar aqui patches de imagem, configmaps, etc.
# Exemplo de patch de imagem:
# images:
#   - name: SEU_NOME_DE_IMAGEM_NO_DEPLOYMENT_BASE
#     newTag: SUA_TAG_DE_IMAGEM_PARA_DEV
